# Pullet 开发状态

最后更新：2026-09-15

本文档是 Pullet 各模块当前状态的事实入口，用于避免依赖聊天记录或压缩摘要判断工程状态。

## 状态标记

- **已确认**：已从当前代码、配置或编译结果核对。
- **用户验收**：由开发者工具或人工操作确认，但未形成自动化测试。
- **待验证**：已有实现，尚未完成目标平台端到端验收。
- **规划中**：尚未实现或还未决定最终方案。

## 工作区

| 用途 | 路径 |
| --- | --- |
| 独立模块源仓库 | `G:\GitHub\PulletFramework` |
| Unity 验证工程 | `G:\XuMingJun\2026\Mini_Games\PulletMiniGameValidation` |
| Unity 版本 | `2022.3.62f3` |

验证工程用于组合安装和平台测试，独立仓库是可复制、可发布模块的源头。修改公共模块后，应同步两处并分别检查差异。

## 模块状态

### PulletFramework

状态：**已确认**

- Window 支持导航栈、弹窗队列、Loading、Toast、返回键拦截、动画和安全区适配。
- 每次窗口打开均有独立身份，关闭、替换、销毁和重新打开不会让异步操作串台或永久等待。
- `PulletResources` 只为框架内的 UI、Form、Sound 和 Pooling 提供资源策略，不替代业务资源系统。
- Form 区分读取结束与全部成功，提供失败明细、失败表重试和严格 TSV 校验；业务格式仍通过 `Parse` 扩展。
- Pooling、UI 和 Sound 已补充异步句柄、释放、取消及包卸载前的生命周期处理。
- Pooling 的 Spawner 具有销毁终态，加载中取消仍完成通知；空句柄、外部 Destroy、失败重试和资源释放已有回归覆盖。
- Sound 的地址与 `AudioClip` 播放共用请求代次；通道、全局和应用后台暂停独立记录，避免迟到加载覆盖或错误恢复。
- HTTP/HTTPS 与 WebSocket 保留通用调度层，并允许小游戏平台注入传输实现。
- `PulletPlayerPrefs` 默认使用 Unity 存储，平台模块可安装无反射的微信或抖音后端。
- `PulletPlayerPrefs` 支持按实例确认和卸载后端，宿主模块退出时不会误删后来由其他模块替换的后端。
- `PLogger` 是 Pullet 模块唯一 Unity 日志出口。`PulletSettings.logLevel` 只控制运行时日志；编辑器构建、上传和配置错误始终输出。

### PulletFramework.YooAsset

状态：**远端发布与抖音开发者工具运行已确认，真机缓存待验证**

- YooAsset 采用独立可选模块，不侵入基础框架。
- `PulletYooAssets` 提供包级门面，支持默认包和多个按需 Package。
- `PulletYooAssetPackagePipeline` 负责文件系统初始化、版本请求、清单更新、可选下载和清理。
- 支持指定 Package 的检查、下载、暂停、恢复、取消、缓存清理、资源卸载和包销毁。
- 业务资源加载仍直接使用 YooAsset `ResourcePackage` API，避免重复封装资源加载接口。
- 编辑器提供 Package 构建、发布报告、供应商配置和腾讯云 COS 上传能力。
- 资源版本与 Player 版本分离；纯资源更新不要求修改 Player 版本。
- 构建发布与运行时下载共用平台目录映射；自动版本策略不会回退到旧远端或旧内置清单。
- 自动 Prepare 在远端失败时可复用当前进程已激活的清单；显式检查、更新和下载仍如实返回失败。冷启动断网仍待平台验收。
- 发布会话冻结供应商配置并在版本指针前校验文件，清单失败或文件变化不会继续发布指针。
- `YooAsset.Extension` 将 YooAsset 内部 Web 策略桥接为公开的平台接口，平台 SDK 程序集不直接依赖 internal 类型。

### PulletMiniGame

状态：**基础平台链路已用户验收，部分线上能力待验证**

- 微信、抖音分别使用独立运行时和编辑器适配程序集。
- 已提供生命周期、登录、分享、激励视频、平台配置、抖音侧边栏复访和排行榜抽象。
- 编辑器发布窗口统一切换目标平台、环境、宏和 SDK 参数。
- 微信和抖音 SDK 绑定放在独立 Sample/桥接层，核心接口不绑定具体 SDK 版本。
- 平台初始化统一按适配器会话共享；失败可重试，单个等待者取消、平台切换和关闭后的迟到结果不会污染当前存储后端。
- 微信开放数据域排行榜和抖音托管排行榜保留平台差异，不伪装成完全一致的数据接口。

### PulletFramework.AssetPublishing

状态：**已确认**

- 当前提供腾讯云 COS 发布和 CORS 配置。
- 发布凭据只用于编辑器，不进入运行时配置加载流程。
- 供应商边界允许后续增加其他对象存储实现。
- YooAsset 与小游戏首包发布使用供应商会话快照；同一供应商的并发发布会被拒绝，避免共享客户端切换配置。

### PulletFramework.HybridCLR

状态：**已完成兼容性评估，当前项目不启用**

- 模块保持独立，不作为小游戏基础依赖。
- 当前验证项目以平台包体和 YooAsset 资源更新为主，不引入代码热更新成本。

## 已完成验证

### 编译

- 2026-09-11：验证工程中 15 个相关运行时、编辑器、平台和示例程序集通过独立 C# 编译检查。
- 本次只做程序集检查，没有触发 PC、WebGL、微信或抖音完整构建。
- Pullet 模块中除 `PLogger` 后端外，已无直接 `UnityEngine.Debug.Log*` 调用。
- 独立模块源仓库已整理并推送至 `main@43e6374`。
- 验证工程已按基础框架、YooAsset、小游戏平台和 Demo 拆分提交，并推送至 `main@1b90a2f`；提交后工作区干净。
- 2026-09-11：YooAsset 版本 `2026-09-11-171340` 已批量发布至 COS。5 个 Bundle、清单、哈希和版本指针的公网状态、长度、SHA-256、CORS 与缓存响应头均通过校验。
- 哈希资源与带版本号清单使用一年 immutable 缓存；可变 `.version` 指针强制重新验证。
- 2026-09-14：修复抖音平台宏不一致、缺少 `TTWebGL` 引用和 WebGL 异常控制流后，开发者工具中
  TT ABFS、YooAsset 远端初始化和首页资源加载通过；不再访问 `dummy.dummy.dummy`。
- 2026-09-15：基础加固 S1-S3 自动化回归达到 EditMode 36/36、PlayMode 4/4；包含自动启动复用当前清单的离线策略边界测试。S3 线上只读检查确认
  WebGL 当前版本指针为 `2026-09-14-150511`，清单、哈希和抽样 Bundle 状态及长度与本地报告一致。
- 2026-09-15：S3 公共改动已同步，版本选择、发布一致性及自动回退的自动化测试已通过；真实平台组合仍按“当前待验证事项”验收。
- 2026-09-15：S4.1 窗口打开身份与取消语义已加固，自动化回归更新为 EditMode 39/39、PlayMode 4/4。
- 2026-09-15：S4.2 Sound 异步请求顺序、暂停层级和应用生命周期已加固，回归更新为 EditMode 39/39、PlayMode 7/7。
- 2026-09-15：S4.3 MiniGame 初始化共享、失败重试、取消、平台切换、关闭后迟到结果及存储后端所有权已加固，回归更新为 EditMode 45/45、PlayMode 7/7。
- 2026-09-15：S4.4 Form 完成状态、失败明细、失败重试、严格 TSV 与销毁中读取已加固，回归更新为 EditMode 45/45、PlayMode 9/9。
- 2026-09-15：S4.5 Pooling 的销毁终态、取消通知、实例与资源所有权及失败重试已加固，回归更新为 EditMode 45/45、PlayMode 15/15。
- 2026-09-15：S5 第一批将 Package/池查找改为字典索引，并为已完成句柄增加即时完成路径；合成查找基准已记录，回归更新为 EditMode 45/45、PlayMode 16/16。真实 Player Profiler 基准仍待执行。
- 2026-09-15：增加缓存命中 PlayMode 基准，100/1000/5000 次约 0.767/6.508/32.395 ms，仅创建一个实例；线程分配计数在当前 Mono 批处理不可用，不据此声明零 GC。PlayMode 回归更新为 17/17。
- 2026-09-15：活跃实例扫描在 100/1000/5000 个实例下执行 100 次约 0.736/3.473/17.240 ms，暂不引入分帧扫描复杂度；PlayMode 回归更新为 18/18。
- 2026-09-15：S6 使用全新临时工程完成三组本地 UPM 安装编译：仅核心、核心 + YooAsset + AssetPublishing、再叠加 MiniGame；修复核心包缺少 Unity 模块依赖，并将验证工程的首包 CDN/内置资源实现同步到独立仓库后复测通过。
- 2026-09-15：CoreOnly 成功构建并实际运行 Windows x64 Player，日志确认框架初始化、运行一帧和销毁均成功，进程退出码为 0。该结果不替代小游戏真机或 Player Profiler 验收。
- 2026-09-15：本轮加固、测试、模块 README 与小游戏首包/内置资源能力已提交并推送：独立模块仓库 `main@e670808`，验证工程 `main@5d94423`。提交前凭据扫描通过，验证工程的 COS 私密配置保持本地忽略。

### 抖音

状态：**用户验收**

- 基础 Demo 已在抖音开发者工具运行。
- 生命周期、登录和分享等基础链路已确认可用。
- 没有真实广告位，因此激励视频尚未完成真实广告验收。
- 当前启动和加载偏慢，尚未通过真机性能数据定位原因。

### 微信

状态：**用户验收**

- 基础 Demo 已在微信开发者工具启动。
- 正式小游戏 AppID：`wx045e11844757f853`。
- 启动失败的关键原因是缺少开放数据域渲染库 Layout 插件 `wx7a727ff7d940bb3f`；添加后恢复。
- Unity 引擎插件 provider 为 `wxe5a48f1ed5f544b7`。
- 详细问题记录见 `PulletMiniGame/Documentation/PlatformTroubleshooting.zh-CN.md`（若路径调整，应同步更新本链接）。

## 关键架构决定

1. 基础框架、YooAsset、小游戏平台、资源发布和 HybridCLR 保持独立模块。
2. 游戏采用单一启动入口，资源包启动统一走 `PulletYooAssets`，不保留旧的双轨初始化 API。
3. 框架只抽象自身需要的资源能力；业务代码直接使用 YooAsset，避免再次包装其完整 API。
4. Player 版本和资源 Package 版本独立管理。资源版本可以使用日期版本或项目自定义递增版本。
5. 编辑器工具日志不受运行时日志等级影响。
6. 未明确要求时，不反复执行耗时的 PC 或 WebGL 平台构建。

## 当前待验证事项

1. 在微信开发者工具重复完成 YooAsset 版本、清单和远端 Bundle 运行验证。
2. 分别在抖音和微信真机记录首启、热启、实际下载量、缓存占用、失败重试和加载耗时。
3. 使用真实广告位验收激励视频的加载、展示、关闭、失败和重复请求。
4. 验收微信开放数据域排行榜与抖音排行榜的真实账号数据。
5. 轮换曾进入版本历史的 COS 访问凭据，并在后续改为本机私有配置或系统凭据存储。
6. 本地 UPM 全新工程组合安装已通过；仍需补 HybridCLR 依赖环境下的安装，以及 Package Manager UI 的移除、重新安装操作回归。

## 下一阶段

优先完成 **YooAsset + COS 远端资源闭环验收**：

1. 建立一个独立的远端测试 Package，放入可明显识别且体积较小的材质、音频或预制体。
2. 从 Workspace 构建 Package，并发布到约定的“应用版本/资源兼容通道”路径。
3. 抖音开发者工具链路已通过；下一步在真机确认二次冷启动不重复下载 Bundle。
4. 使用同一套 Package 在微信重复验证，记录两端文件系统和缓存行为差异。
5. 为流水线补充必要的耗时、下载字节数、错误阶段和缓存命中诊断，不添加逐文件噪声日志。

该阶段完成后，再进行真机性能优化和模板化发布验收。这样可以先证明资源架构真正可用，再继续扩展游戏功能。

## 维护规则

- 每个阶段结束时更新“已完成验证”和“当前待验证事项”。
- 只有代码、配置、编译输出或人工验收能把事项标记为“已确认”或“用户验收”。
- 来自聊天摘要但未重新核对的内容必须标记为“待验证”。
- 重大决策记录原因和日期，不只记录最终结论。
- 文档与代码冲突时，以当前代码和可复现验证结果为准，并立即修正文档。
